<% require 'cloudflare/cloudflare' %>

<div
  class="cf-turnstile"
  data-sitekey="<%= Rails.application.credentials.turnstile_site_key || Cloudflare::TURNSTILE_SITE_KEY %>"
  <% if defined?(callback) %> data-callback="<%= callback %>"<% end %>
  data-theme="light">
</div>
