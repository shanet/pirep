<%= javascript_tag nonce: true do %>
  document.addEventListener('DOMContentLoaded', () => {
    <% flash.each do |type, message| %>
      window.showFlash("<%= flash_to_class(type)[:class] %>", "<%= message %>");
    <% end %>
  }, {once: true});
<% end %>
